#include <bits/stdc++.h>
using namespace std;

int main() {
	// your code goes here
	int t=0;
	cin>>t;
	while(t--){
	    int n=0;
	    cin>>n;
	    vector<int> a(n);
	    vector<int> pref(n);
	    int count=0;
	    
	    for(int i=0;i<n;i++){
	        cin>>a[i];
	        
	        if(a[i]==2){
	            count++;
	        }
	        pref[i]=count;
}
        if(count%2!=0){
            cout<<-1<<endl;
            continue;
        }
        int idx=0;
    
            for(int i=0;i<n;i++){
                if(pref[i]==count/2){
                idx=i;
                break;}
        }
        cout<<idx+1<<endl;

  }
  
  
}
